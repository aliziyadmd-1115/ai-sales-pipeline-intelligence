# Resume bullets

Use after running the project and being able to explain its design:

- Built an end-to-end synthetic CRM analytics platform with Python, pandas, scikit-learn, FastAPI, and Streamlit to validate 3,000 opportunities, redact emails, estimate win probability, and retrieve historical sales evidence.
- Evaluated a text-and-structured-feature classifier on separate training, validation, and test sets, achieving 0.790 holdout ROC-AUC and 0.177 Brier score versus a 0.233 probability baseline; exported split IDs, row-level predictions, calibration plots, and threshold tradeoffs.
- Added 65 regression tests covering malformed data, API readiness, retrieval abstention, stale-index prevention, and LLM fallback; configured GitHub Actions to regenerate evaluation evidence and smoke-test a running Docker API.

Describe ChromaDB and local Ollama as optional integrations. Their live services need separate verification. These are synthetic benchmark results, not production sales results or internship work.
